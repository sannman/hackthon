## FocusFlow — smart adaptive study planner

This app builds a live-updating study plan that reacts to real life: available hours, missed sessions, and exam pressure. It ships with a shadcn-inspired UI, focus mode timer, priority engine, and interactive timeline.

### Running the app

```bash
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000).
